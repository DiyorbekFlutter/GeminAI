import 'package:c_group_chat_with_ai/stateless_widget.dart';

import '../../services/io_service.dart';

class ShowEmail extends StatelessWidget {
  @override
  Future<void> build() async {
    IO.blue("${IO.t(11)}    diyorbekflutter@gmail.com");
    IO.blue("${IO.t(11)}<<----------------------------->>");
    IO.blue("${IO.t(11)}       << ---  |||  --- >> ");
    IO.n(10);
  }
}
